export function o2h(obj: any, encodeAndWrite: (s: string) => void){
    const o2hInstance = new O2H()
}
export class O2H {
    constructor(public obj: any, public encodeAndWrite: (s: string) => void) {
        encodeAndWrite.
    }
}