{
    "wrapper": ["<form>", "|", "</form>"],
    "objectProp": ["<details><summary>${label}</summary>", "</details>"],
    "stringProp": "<label>${label}: <input type='text' name='${path}' value='${value}' ${mib}></label>",
    "numberProp": "<label>${label}: <input type='number'  name='${path}'  value='${value}' ${mib}></label>",
    "booleanProp": "<label>${label}: <input type='checkbox'  name='${path}' ${checked} ${mib}></label>",
    "arrayProp": ["<details><summary>${label}</summary>", "</details>"],
    "arraySummaryPath": "name"
}